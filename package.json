{
  "name": "natron-ia",
  "version": "1.0.0",
  "description": "Natron IA - Plataforma de Desenvolvimento Pessoal Gamificada",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "postinstall": "cd backend && npm install && npx prisma generate"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mysql2": "^3.22.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "node-ssh": "^13.2.1"
  }
}
